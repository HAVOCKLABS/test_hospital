cat > render.yaml <<'EOF'
services:
  - type: web
    name: hospital
    env: python
    pythonVersion: 3.11.9
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn routes:app"
EOF
